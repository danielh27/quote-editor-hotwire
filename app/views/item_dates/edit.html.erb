<main class="container">
  <%= link_to "Back to quote", @quote %>

  <div class="header">
    <h1>Edit date</h1>
  </div>

  <%= turbo_frame_tag @item_date do %>
    <%= render "form", quote: @quote, item_date: @item_date %>
  <% end %>
</main>
